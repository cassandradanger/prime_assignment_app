<% content_for :page_title, "Change Password :: #{@admin.name}" %>

<div class="ibox-content">
  <div class="row m-b-md">
    <div class="col-md-4">
      <%= render 'edit_password_form' %>
    </div>
  </div>
</div>